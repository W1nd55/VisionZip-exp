# SparseZip Requirements for A40 GPU
# CUDA 12.1 + PyTorch 2.1.2

--extra-index-url https://pypi.org/simple
--index-url https://download.pytorch.org/whl/cu121

# Core PyTorch
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# Additional dependencies
protobuf
datasets
huggingface_hub
scikit-optimize
pandas

# ============================================
# Installation Instructions
# ============================================

# 1. Install flash-attn 2.x for A40 (compute capability 8.6):
#    conda install -c nvidia cuda-toolkit=12.1 -y
#    export TORCH_CUDA_ARCH_LIST="8.6"
#    export FLASH_ATTENTION_SKIP_SM90=1
#    pip install "flash-attn==2.5.6" --no-build-isolation --no-cache-dir

# 2. Install LLaVA:
#    pip install -e models/LLaVA --no-deps

# 3. Install VisionZip:
#    Modify models/VisionZip/setup.py:
#      packages=find_packages(include=["visionzip", "visionzip.*"])
#    pip install -e models/VisionZip --no-deps
